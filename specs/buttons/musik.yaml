# Erster echter Test-Button — stateful musik-toggle auf Slot 7.
# E2E-Test des kompletten Scaffolder-Pipelines (alle 4 Bloecke).

id: musik
label: "MUSIK"
page: CTRL
slot: 7
endpoint: /api/musik/toggle
state:
  endpoint: /api/musik
  poll_interval_s: 5
  response_field: musik
  active_value: true
  active_label: "PLAYING"
  idle_label:   "PAUSED"
